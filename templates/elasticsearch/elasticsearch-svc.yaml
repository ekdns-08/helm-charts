apiVersion: v1
kind: Service
metadata:
  name: {{ include "nls-rabbitmq.fullname" . }}-service
  namespace: {{ .Release.Namespace }}
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: {{ .Values.livenessProbe.path }}
  labels:
    app: {{ include "nls-rabbitmq.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "nls-rabbitmq.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      nodePort: {{ .Values.service.nodePort }}
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 1800
