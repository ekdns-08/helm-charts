# startup Porbe 적용
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nls-elasticsearch-deployment
  namespace: test
  labels:
    app: nls-rabbitmq
  annotations:
    kubernetes.io/change-cause: seoulbigdata-registry.ncr.gov-ntruss.com/nls-docker-rabbitmq:3.9.13-debian-10-r26
spec:
  replicas: 1
  progressDeadlineSeconds: 120
  revisionHistoryLimit: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: nls-rabbitmq
  template:
    metadata:
      labels:
        app: nls-rabbitmq
    spec:
      containers:
        - name: elasticsearch
          image: seoulbigdata-registry.ncr.gov-ntruss.com/nls-docker-rabbitmq:3.9.13-debian-10-r26
          imagePullPolicy: Always
          ports:
            - containerPort: 5672
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 1200Mi
          livenessProbe:
            initialDelaySeconds: 30
            periodSeconds: 10
            httpGet:
              path: /
              port: 5672
          readinessProbe:
            initialDelaySeconds: 30
            periodSeconds: 10
            httpGet:
              path: /
              port: 5672
          startupProbe:
            tcpSocket:
              port: 5672
            failureThreshold: 30
            periodSeconds: 10
      imagePullSecrets:
        - name: regsecret
