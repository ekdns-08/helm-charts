apiVersion: v1
kind: Service
metadata:
  name: redis-service
spec:
  type: {{ .Values.redis.service.type }}
  ports:
    - port: {{ .Values.redis.service.port }}
      targetPort: 6379
      nodePort: {{ .Values.redis.service.nodePort }}
  selector:
    app: redis
